# 私有记忆 / 工作区元数据（不含源码，勿上传）
.workbuddy/

# 临时测试目录
.smoke/

# 操作系统 / 编辑器
Thumbs.db
.DS_Store
*.log

# 本地服务器缓存
npmcache/

# 根目录 jsdom 测试残留（应用无需；后端依赖在 server/）
/node_modules/
/package.json
/package-lock.json
/test_*.js
